const logLevelData = {
  "*": "silent", //default log level for all modules
  //need to specify a level for each module, ie
  //the wildcard logs nothing but home logs info
};

export default logLevelData;
